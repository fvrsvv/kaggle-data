source .venv/bin/activate

# #!/usr/bin/env bash

# # Автоматическая активация Python venv
# if [ -d ".venv" ]; then
#     source .venv/bin/activate
#     echo "✅ Activated virtual environment: .venv (Python $(python --version))"
# else
#     echo "⚠️  .venv directory not found. Run 'uv venv --python 3.12 .venv' or 'python -m venv .venv'"
# fi